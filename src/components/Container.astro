---
const { class: className, id } = Astro.props;
---

<section
  id={id}
  data-section={id}
  class={`section ${className || ""} scroll-mt-20 w-full mx-auto px-4 container lg:max-w-4xl md:max-w-2xl`}
>
  <slot />
</section>
